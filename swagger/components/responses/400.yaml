# swagger/components/responses/401.yaml
description: Not valid id
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Not valid id
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Not valid id'
